{
  "name": "aws-diagram",
  "private": true,
  "scripts": {
    "build": "dot diagram.dot -Tsvg -o diagram.svg",
    "watch": "onchange -i diagram.dot -- npm run build"
  },
  "dependencies": {
    "onchange": "^3.3.0"
  }
}
